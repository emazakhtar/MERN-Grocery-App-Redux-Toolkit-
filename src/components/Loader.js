// Loader.js
import React from "react";
import "./Loader.css"; // Import the CSS file containing the loader styles

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;
